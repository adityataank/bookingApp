<h1>Booking Details</h1>
<p>
    place : <%= @booking.place%><br>
    movie : <%= @booking.movie%><br>
    date : <%= @booking.date%><br>
    time : <%= @booking.time%><br>
    <%if current_user.role == 'admin'%>
        payment_proof : <%= image_tag @booking.payment_proof, width: "20%" , height: "20%"%>
    <%end%>
</p>
<p>
     <%= link_to "Download Ticket" , download_pdf_path%>  ||
    <%= link_to "My Bookings", bookings_path%>  || 
    <%= link_to "New Booking" , new_booking_path%>  || 
    <% if current_user.role == "admin"%>
        <%# <%= link_to "Delete", booking_path(@booking), :confirm => "Are you sure ?", :method => :delete %> 
        <%= link_to "Delete", booking_path(@booking), data: { confirm: 'Are You sure?' }, :method => :delete %>
    <% end %>
    
</p>